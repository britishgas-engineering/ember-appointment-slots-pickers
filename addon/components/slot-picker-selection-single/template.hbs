{{scroll-anchor name="appointment-selected"}}

{{#if selected}}
  <p class="bold font-size-6 mt4">
    Arriving {{selected.longDayLabel}} between
    {{selected.startTimeLabel}} and {{selected.endTimeLabel}}
  </p>
{{/if}}

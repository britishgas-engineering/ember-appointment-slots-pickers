{{#if isSelected}}
  {{#if canSelectMultipleSlots}}
    <BgButton
      class="asp-btn selected background background-dark-blue"
      @theme="secondary"
      @hide-icon="true"
      @action={{action attrs.deselect this.appointmentSlot}}
    >
      {{appointmentSlot.slotPickerTime}}
    </BgButton>
  {{else}}
    <div class="asp-appointment-slot-selected white background background-dark-blue">
      {{appointmentSlot.slotPickerTime}}
      {{#if appointmentSlot.slotPickerHasTag}}
        <span class="fa fa-tag" aria-labelledby="tagInfo"></span>
      {{/if}}
      <span class="fa fa-check" aria-hidden="true"></span>
    </div>
  {{/if}}
{{else}}
  <BgButton
    class="asp-btn"
    @theme="secondary"
    @hide-icon="true"
    @isTagged={{this.appointmentSlot.slotPickerHasTag}}
    @action={{action attrs.select this.appointmentSlot}}
  >
    {{appointmentSlot.slotPickerTime}}
  </BgButton>
{{/if}}
